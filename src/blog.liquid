---
layout: top-level
title: Blog
header-info-display: true
header-info: Blog
---

<section>

    <div style="display: flex; flex-direction: column; gap: 24px; width: 100%; grid-column: 1/11;">
        {%- for post in collections.post reverse -%}
            <div>
                <h3><a href="{{ post.url }}"> {{ post.data.title }}</a></h3>
                <p>Posted on {{ post.date | date: "%b %d, %Y" }}.</p>
            </div>
        {%- endfor -%}
    </div>

</section>
